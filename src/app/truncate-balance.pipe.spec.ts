import { TruncateBalancePipe } from './truncate-balance.pipe';

describe('TruncateBalancePipe', () => {
  it('create an instance', () => {
    const pipe = new TruncateBalancePipe();
    expect(pipe).toBeTruthy();
  });
});
